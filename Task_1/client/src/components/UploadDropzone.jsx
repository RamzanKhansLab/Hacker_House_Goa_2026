import { useRef, useState } from "react";
export default function UploadDropzone({ onFile, processing, compact = false, fileName }) {
  const inputRef = useRef(null); const [dragging, setDragging] = useState(false); const processFiles = (files) => { const file = files?.[0]; if (file) onFile(file); };
  return <div className={`upload-dropzone ${compact ? "upload-dropzone--compact" : ""} ${dragging ? "is-dragging" : ""}`} onDragOver={(event) => { event.preventDefault(); setDragging(true); }} onDragLeave={() => setDragging(false)} onDrop={(event) => { event.preventDefault(); setDragging(false); processFiles(event.dataTransfer.files); }}><input ref={inputRef} className="sr-only" type="file" accept=".jpg,.jpeg,.png,.heic,.heif,image/jpeg,image/png,image/heic,image/heif" capture="user" onChange={(event) => { processFiles(event.target.files); event.target.value = ""; }} /><div className="upload-icon" aria-hidden="true">+</div><div className="upload-copy"><strong>{processing ? "Processing photo…" : compact ? "Swap your portrait" : "Drop in your portrait"}</strong><span>{processing ? "Building your local preview" : fileName || "JPG, PNG, HEIC — straight from your device"}</span></div><button type="button" className="button button--upload" onClick={() => !processing && inputRef.current?.click()} disabled={processing}>{processing ? "BUILDING…" : compact ? "REPLACE" : "CHOOSE A PHOTO"}</button>{!compact && <p className="upload-hint">YOUR FACE, YOUR SIGNAL <span>·</span> UP TO 20 MB</p>}</div>;
}
